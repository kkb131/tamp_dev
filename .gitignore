# Docker build artifacts (persisted from container)
.docker/build/
.docker/install/
.docker/log/

# Python
__pycache__/
*.pyc
*.pyo

# ROS2 build artifacts (if building on host)
build/
install/
log/

# Claude Code local data
.docker/claude/

# Manus SDK binary libs (too large for GitHub, download separately)
manus/sdk/SDKClient_Linux/ManusSDK/lib/*.so

# Manus SDK build artifacts
manus/sdk/SDKClient_Linux/build/
manus/sdk/SDKClient_Linux/*.out
